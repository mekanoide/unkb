export default defineNuxtRouteMiddleware((to, from) => {
    // TODO: Definir un middleware que podamos añadir
    // a las rutas que queramos proteger con autenticación
    // y que redirija a la página de login si no estamos
    // autenticados
})
